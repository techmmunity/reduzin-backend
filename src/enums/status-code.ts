/* eslint-disable @typescript-eslint/no-magic-numbers */

export enum StatusCodeEnum {
	SUCCESS = 200,
	CREATED = 201,
	REDIRECT = 301,
	BAD_REQUEST = 400,
	UNAUTHORIZED = 401,
	FORBIDDEN = 403,
	NOT_FOUND = 404,
	CONFLICT = 409,
	INTERNAL = 500,
}
